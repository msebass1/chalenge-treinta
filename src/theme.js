import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#FED209',
    },
    secondary: {
      main: '#47525E',
    },
  },
  bg:{
    yellow:'#FED209'
  }
});
export default theme;
